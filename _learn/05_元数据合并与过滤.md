# 第 5 课自测：元数据合并与过滤

**涉及文件**：`app/agent/nodes/merge_retrieved_info.py` · `filter_table.py` · `filter_metric.py` · `app/prompt/` 目录

---

## ⚡ 实战任务

打开 `merge_retrieved_info.py`，找到它最终写入 State 的字段名，全部列出来。

然后打开 `filter_table.py`，找到它调用的 Prompt 文件路径，打开那个 Prompt，回答：LLM 收到的输入里有哪几类信息？要求 LLM 输出的格式是什么（具体结构，不是「JSON」这两个字）？

→ 预期：能说出 merge 写入的具体字段名；能描述 filter_table Prompt 的输入结构和输出契约。Prompt 的输出格式说不清，面试时「用 LLM 做过滤」就是空话。

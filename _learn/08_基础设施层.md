# 第 8 课自测：基础设施层

**涉及文件**：`app/agent/context.py` · `app/clients/` 目录 · `app/repositories/` 目录

---

## ⚡ 实战任务

打开 `app/clients/` 目录，列出所有 client 文件，每个 client 连接的是什么服务（一行一个：`client名 → 服务名`）。

然后打开 `app/agent/context.py`，数出 Context 里有几个 repository 字段，把它们的字段名写下来。

最后回答一个设计题：为什么 `get_meta_session` 和 `get_dw_session` 要分成两个函数返回两个独立的 session，而不是共用一个 MySQL session？

→ 预期：能完整列出 client→服务的映射；能说出 context 里的 repository 字段名；能说出两个 session 分开的具体原因（不是「职责分离」，是什么场景下共用会出问题）。

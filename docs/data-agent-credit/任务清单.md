# 信贷改造任务清单

> 按顺序执行，每完成一步打勾。
> 遇到问题先看 `参考/data-agent-spec.md`，再问 Claude。

---

## 阶段一：数据基础（B1-B3）

完成后验证：重建 docker 数据库 + 重跑 `build_meta_knowledge.py` + 发一条测试查询

---

### B1 — 替换建表 SQL

**文件**：`docker/mysql/init/02_create_dw_tables.sql`

**操作**：全部替换，删掉电商5张表，换成以下8张信贷表：

- [x] `dim_product_type` — 产品维度（3条记录）
- [x] `dim_channel` — 渠道维度（5条记录）
- [x] `dim_date` — 时间维度（保留结构，换信贷日期）
- [x] `dwd_loan` — 放款事实表（核心，30000条由脚本生成）
- [x] `dwd_repayment` — 还款记录表
- [x] `dwd_overdue` — 逾期记录表
- [x] `ads_loan_monthly` — 月度放款汇总（ADS层）
- [x] `ads_overdue_monthly` — 月度逾期汇总（ADS层）

**验证**：`docker-compose down -v && docker-compose up -d`，进 MySQL 确认8张表存在。

---

### B2 — 生成模拟数据

**文件**：`scripts/generate_credit_data.py`（新增）

**操作**：新建脚本，生成并写入：
- [x] 30000条 `dwd_loan` 放款记录（6个月，3种产品，5个渠道）
- [x] 对应的 `dwd_repayment` 还款记录（每笔贷款按期数生成还款计划）
- [x] 逾期记录写入 `dwd_overdue`（逾期率约5-8%）
- [x] 聚合汇总写入 `ads_loan_monthly` 和 `ads_overdue_monthly`

**验证**：运行脚本后，`SELECT COUNT(*) FROM dwd_loan` 返回约30000。

---

### B3 — 改造元数据

**文件**：`conf/meta_config.yaml`

**操作**：全部替换，换成信贷业务元数据：
- [x] 8张表的定义（含 `layer` 字段：ADS/DWS/DWD）
- [x] 每列的 `description`、`alias`、`examples` 要充分（决定召回质量）
- [x] 信贷指标定义（见下方指标清单）
- [x] 歧义指标加 `ambiguous: true`（逾期率）

**信贷指标清单**：

| 指标名 | 口径 | ambiguous |
|--------|------|-----------|
| 放款金额 | SUM(loan_amount) | false |
| 放款笔数 | COUNT(loan_id) | false |
| 平均放款金额 | AVG(loan_amount) | false |
| 逾期率 | 逾期金额/应还金额（需澄清口径） | **true** |
| 首逾率(M1) | 首次逾期笔数/到期笔数 | false |
| 在贷逾期率 | 当前逾期余额/在贷余额 | false |
| 回收率 | 实收金额/应收金额 | false |
| 平均逾期天数 | AVG(overdue_days) | false |

**验证**：运行 `python app/scripts/build_meta_knowledge.py`，无报错。

---

### 阶段一验证 ✓

- [ ] 发送测试查询："上个月各渠道的放款总额是多少"
- [ ] 确认 SSE 流程正常，最后返回 `{"type": "result", "data": [...]}`
- [ ] 数据结果合理（不是0行，不是SQL报错）

---

## 阶段二：新增节点（B4-B6）

完成后验证：口径澄清和多轮对话功能

---

### B4 — 新增 clarify_query 节点

#### 4.1 修改 state.py
**文件**：`app/agent/state.py`

- [ ] 在 `DataAgentState` 中新增字段：
  ```python
  clarification_question: str | None  # 需要澄清时的追问话术，否则为None
  ```

#### 4.2 新增节点文件
**文件**：`app/agent/nodes/clarify_query.py`（新增）

**B类描述**（写好后让 Claude 生成代码）：
```
输入：state 里的 metric_infos（list[MetricInfoState]）和 query（str）
逻辑：
  - 定义字典 _AMBIGUOUS_METRICS，key=指标名，value=追问话术
    示例：{"逾期率": "您查询的逾期率是指首逾率(M1)还是在贷逾期率？"}
  - 遍历 metric_infos，检查 metric.name 是否在字典里
  - 若在：检查 query.lower() 是否包含澄清词（首逾/M1/M2/在贷/累计）
  - 若不含澄清词：把追问话术写入 clarification_question，通过 writer 发送事件，返回
  - 否则：写入 None
输出：state 的 clarification_question 字段（str | None）
```

- [ ] 写 B 类描述（参考上方模板，结合实际指标填写）
- [ ] 让 Claude 生成代码
- [ ] 逐行读懂，合上复述，改一处验证

#### 4.3 修改 graph.py
**文件**：`app/agent/graph.py`

- [ ] `add_node("clarify_query", clarify_query)`
- [ ] 删除：`add_edge("add_extra_context", "generate_sql")`
- [ ] 新增：`add_edge("add_extra_context", "clarify_query")`
- [ ] 新增条件边：
  ```python
  add_conditional_edges(
      "clarify_query",
      lambda state: END if state["clarification_question"] else "generate_sql",
      {END: END, "generate_sql": "generate_sql"}
  )
  ```

**验证**：发送"逾期率是多少"，确认返回追问而不是 SQL 结果。

---

### B5 — 新增 rewrite_query 节点

#### 5.1 修改 state.py
**文件**：`app/agent/state.py`

- [ ] 在 `DataAgentState` 中新增字段：
  ```python
  conversation_history: list[dict]  # 格式：[{"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]
  ```

#### 5.2 新增节点文件
**文件**：`app/agent/nodes/rewrite_query.py`（新增）

**B类描述**：
```
输入：state 里的 query（str）和 conversation_history（list[dict]）
逻辑：
  - 如果 conversation_history 为空，直接返回原 query（不需要改写）
  - 否则：取最近3轮对话（最多6条消息）
  - 构造 prompt：把历史和当前 query 发给 LLM，让它把 query 改写成完整、
    自包含的句子（把"这个"、"上周呢"等指代替换成具体内容）
  - 如果 query 已经是完整问题，LLM 原样返回
  - 返回改写后的 query
输出：更新 state 的 query 字段（str）
```

- [ ] 写 B 类描述（结合实际历史格式填写）
- [ ] 让 Claude 生成代码
- [ ] 逐行读懂，合上复述，改一处验证

#### 5.3 修改 graph.py
**文件**：`app/agent/graph.py`

- [ ] `add_node("rewrite_query", rewrite_query)`
- [ ] 删除：`add_edge(START, "extract_keywords")`
- [ ] 新增：`add_edge(START, "rewrite_query")`
- [ ] 新增：`add_edge("rewrite_query", "extract_keywords")`

#### 5.4 修改 API Schema
**文件**：`app/api/schemas/query_schema.py`

- [ ] 新增字段：
  ```python
  conversation_history: list[dict] = []
  ```

#### 5.5 修改路由
**文件**：`app/api/routers/query_router.py`

- [ ] 初始化 state 时把 `request.conversation_history` 传入

**验证**：第一轮问"各渠道放款额"，第二轮问"上周呢？"，确认第二轮能正确改写并查出结果。

---

### B6 — 更新 filter_table prompt

**文件**：`prompts/filter_table_info.prompt`

- [ ] 在 prompt 中加入分层路由规则：
  ```
  表选择优先级（从高到低）：
  1. ADS层表（已有聚合结果，查询代价最低）
  2. DWS层表（按维度汇总，中等代价）
  3. DWD层表（原始明细，仅在必须查个体记录时使用）
  
  若 ADS 层已有满足查询需求的聚合数据，禁止选择 DWD 层表。
  ```

**验证**：问"上月逾期率"，确认 SQL 查的是 `ads_overdue_monthly` 而不是 `dwd_overdue`。

---

## 阶段三：测试迭代（B7）

### B7 — 跑30个测试用例

**文件**：`tests/test_credit_queries.py`（新增）

测试用例分类：

| 类别 | 数量 | 示例 |
|------|------|------|
| 简单聚合（ADS层） | 8个 | "上个月各渠道放款总额" |
| 多维分析（DWS层） | 8个 | "消费分期12期产品各月放款趋势" |
| 明细查询（DWD层） | 4个 | "找出逾期超过30天的借款人" |
| 口径澄清触发 | 5个 | "逾期率是多少"（应触发追问） |
| 多轮对话 | 5个 | 第二轮含"上周呢？"（应改写成功） |

- [ ] 编写30个测试查询
- [ ] 逐个运行，记录通过/失败
- [ ] 失败的查询分析原因（召回问题/元数据描述问题/prompt问题）
- [ ] 迭代 `meta_config.yaml` 修复召回问题
- [ ] 迭代 prompt 修复过滤/生成问题

**目标**：30个用例中至少25个通过（SQL正确且结果合理）。

---

## 进度记录

| 步骤 | 状态 | 备注 |
|------|------|------|
| B1 建表SQL | ✅ 完成 | |
| B2 生成数据 | ✅ 完成 | |
| B3 改元数据 | ✅ 完成 | |
| 阶段一验证 | ⬜ 未开始 | |
| B4 clarify_query | ⬜ 未开始 | |
| B5 rewrite_query | ⬜ 未开始 | |
| B6 更新prompt | ⬜ 未开始 | |
| B7 测试迭代 | ⬜ 未开始 | 目标25/30通过 |
